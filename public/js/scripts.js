const body = document.querySelector('body');

const bodyCStyle = window.getComputedStyle(body);

// body.innerText = `font ${bodyCStyle.font}\n line-height: ${bodyCStyle.lineHeight}`;
// body.style.fontSize = '62.5%';

// body.innerHTML = window.location;
